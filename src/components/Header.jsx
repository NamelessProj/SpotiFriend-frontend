import {Typography} from "@material-tailwind/react";

const Header = () => {
    return (
        <header>
            <Typography variant="h1" as="p">
                SpotiFriend
            </Typography>
        </header>
    );
};

export default Header;